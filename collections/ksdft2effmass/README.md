# `ksdft2effmass` literature-review seed

## Status

Bootstrap inventory for human review. It is not a verified bibliography or a
scientific synthesis.

The source application was inspected read-only at revision
`64e28463a909777771f4f74b59ed4508911b63d4`. Its bibliography and manuscripts
were not changed.

## Inventory

- 116 entries occur across three source BibTeX files.
- They collapse to 90 distinct existing citation keys.
- `seed.bib` preserves the application keys and prefers the monograph record
  when a key occurs in more than one source bibliography.
- 1,929 local PDF candidates were scanned across selected scientific reference
  trees, active Obsidian vaults, and Downloads.
- 22 strong local PDF matches were copied into the private Project Koios
  working vault under the matching citation-key filenames.
- 68 references had no retained strong match in the incomplete legacy pass.
  This is not a definitive `not-located` result.
- 90 reference-note stubs were created in the private working vault.

## Files

- `seed.bib` — read-only bibliography snapshot;
- `corpus.csv` — review queue and note/PDF status;
- `source-discovery.json` — preserved privacy-reduced legacy discovery evidence
  and checksums;
- `coverage-observation.json` — content-identified typed projection of that
  legacy evidence, explicitly classified as `incomplete` with ambiguity not
  evaluated.

Tracked reports use aliases and paths relative to searched source roots. They do
not disclose absolute home-directory paths. Personal and finance vault areas
were not searched.

## Authority

Bibliography presence does not mean a source has been read or supports a claim.
The four sources reported as read in the source project's citation audit retain
that status as a report, not as independent Project Koios verification.

Accepted scientific synthesis remains owned by `ksdft2effmass`. This collection
owns only reusable reference identity and review state.
