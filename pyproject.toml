[project]
name = "projectkoios-references"
version = "0.0.0"
description = "reference management and citation handling"
readme = "README.md"
requires-python = ">=3.14"
license = "Apache-2.0"
authors = [
    { name = "Eugene Ragasa", email = "eugene@projectkoios.com" },
]
dependencies = ["pybtex>=0.25"]

[project.scripts]
koios-ref = "projectkoios.references.cli:main"

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src/python"]
namespaces = true

[tool.setuptools.package-data]
"projectkoios.references" = ["py.typed"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.8", "mypy>=1.13"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 80
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
mypy_path = "src/python"
namespace_packages = true
explicit_package_bases = true

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
